FROM node:18-alpine

WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./
RUN npm install

# Copy entire project
COPY . .

# Build TypeScript
RUN npx tsc

# Expose server port (update if different)
EXPOSE 4001

# Start server
CMD ["npm", "run", "server"]
